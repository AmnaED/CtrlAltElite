{
  "name": "hardware",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build  && cd .. && rm -rf server/static && mv client/build server/static"
  }
}
